#include <iostream>

using namespace std;

  int main() {

    /* Write a C++ program to prompt the user to input his/her name and print this name on screen as shown below.
       The text from the keyboard can be read by using cin and to display the text on the screen you can use cout.
   */

   string name;

   cin >> name;
   cout << "\nHello " << name << "!" << endl;


  return 0;
  }
